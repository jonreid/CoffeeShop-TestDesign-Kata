{
    "name": "coffeeshop-testdesign-kata",
    "type": "module",
    "private": true,
    "scripts": {
        "test": "bun test",
        "typecheck": "bunx tsgo --noEmit",
        "format": "biome format --write . && dprint fmt README.md && dprint fmt ../.github/workflows/*.yml",
        "check": "bunx tsgo --noEmit && biome check ."
    },
    "devDependencies": {
        "@biomejs/biome": "^1.9.4",
        "@types/bun": "latest",
        "@typescript/native-preview": "^7.0.0-dev.20250605.1"
    },
    "peerDependencies": {
        "typescript": "^5"
    }
}
